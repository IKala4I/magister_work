# P7 manual verification — Feedback loop

> Honesty rule (CLAUDE.md "Simulator evidence"): everything below ran on the development Mac
> (jest/Deno/pytest) or in CI's Postgres (pgTAP). Nothing ran on a handset, and nothing reached
> a live RecSys service — the service has no host (ADR-0009, owner decision). Each section says
> what it establishes and what it does not.

## 1. Gates (2026-08-27, `phase/P7-feedback-loop`)

| Gate                                                    | Result                                                                                                                           |
| ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm typecheck` · `pnpm lint` · `pnpm format:check`    | clean                                                                                                                            |
| `pnpm test` (apps/mobile jest)                          | **288 passed / 37 suites** (P6: 265) — +11 DAO, +6 Focus, +6 Today, schema/shell/rootLayout adjusted                             |
| `npx expo-doctor` (apps/mobile)                         | 21/21 after `expo install --fix` (SDK 57 patch drift: expo 57.0.17, RN 0.86.3, jest-expo 57.0.5, `@expo/metro-runtime` pinned)   |
| `deno fmt --check` · `deno lint` · `deno check` · tests | clean · clean · clean · **92 passed** (P6: 50) — +25 rewards mapping, +5 override target, +16 attribute-rewards handler, +pins   |
| `uv run ruff check` · `mypy src tests` · `pytest --cov` | clean · clean · **135 passed, 7 skipped** (PostgresRepo — CI only), coverage **92 %** (P6: 126)                                  |
| pgTAP `supabase test db`                                | `p7_feedback_test.sql` **29 assertions** — runs in CI's db job (no Docker on the dev Mac); not executed locally                  |
| `pnpm -r` regenerated types                             | `api.ts` unchanged (the `/feedback` contract did not change); `database.ts` regenerated by CI's contract-sync step on the branch |

## 2. What the tests establish (requirement → evidence)

- **specs/07 §3.4.1 rows 1–10** — one Deno test per row in `_shared/rewards_test.ts` (in-window
  finish 1.0; done-in-slot 1.0; abandoned ≥ 50 % 1.0; abandoned < 50 % r = f; Σ over sessions;
  ±15 min boundary at 15:01; off-slot 0.3 daily-only; late-start finish = off-slot; lapse 0.0
  with `status = lapsed`; another day's completion ≠ this block; skip 0.0 → `rejected`; facts beat
  plans; override pair 0.1/0.7 with origin/target features; one pair per placement; displaced /
  expired rows never produce a tuple; `conflict_flag` → excluded with value kept; idempotent
  re-run; correction inside/outside the 7-day window; ratings and `lapse_observed` never reward).
- **§3.4.2 23:55-local authority, DST** — `p7_feedback_test.sql`: r1 due at exactly 23:55:00 EEST
  and not 23:54:59; r2 (slot ending 01:30 on the fall-back day) due only at 23:55 **EET** on the
  25th, not at the old +03 boundary; r4 due at 23:55 EEST on the spring-forward day; a Los
  Angeles row in the same sweep at 06:55Z; displaced/attributed/expired never due; service-only
  execute; cron job present; tick is a no-op without Vault secrets; `duration_estimates` RLS.
- **Two-phase semantics + outage** — `handler_test.ts`: instant writes+delivers; backend-key
  path (P8's sync-resolve); service down → tuple stored undelivered, re-run does not duplicate,
  the daily sweep delivers once the service answers; `not_configured` (no host) keeps tuples
  pending without error; move → pair + computed target (MO.wd.fresh at 10:00 Kyiv) + row moved;
  daily slice → off_slot/lapsed with statuses and `attributed_at`, re-run is a no-op; correction
  rewrites in place and re-sends with `correction = true` and the original `attributed_at`;
  duration estimator folds finished sessions once (monotone marker).
- **Service math** — `test_blend.py`: River (Squared loss, lr/2) equals the unprojected step to
  1e-12 over three cases and a two-step trajectory; the 2-simplex projection equals a brute-force
  nearest point; `test_feedback.py`: the blend moves toward the reward, persists with the batch
  version, ignores excluded tuples, and a correction-triggered rebuild reproduces the online
  trajectory to 1e-12; `test_energy.py`: rung-2 helpers incl. decay relaxing a personal cell.
  Pre-existing P5 tests still pin SM ≡ naive inverse, rebuild ≡ sequential updates, decay
  half-life, rebuild-after-correction ≠ downdate.
- **Client facts** — `feedbackDao.test.ts` on real SQLite: session lifecycle with a pause
  (focused ms excludes the pause; task done; four facts through the outbox with shared op_ids);
  abandon leaves the task open; rating is a label; Done/Skip/Move/"I did it" payloads; lapse scan
  marks only ended, session-less, undone blocks and is idempotent; **DST**: on the Kyiv fall-back
  night the pre-change 03:30–04:30 slot lapses at the second 04:00 while the post-change one does
  not; third consecutive lapse/skip surfaces the diagnostic; no payload carries the title, no
  payload has a `reward` key; server patches mirror status/slots/features.
- **UI** — `today.test.tsx`: action row, Start → Focus tab, Move picker with 15-min snap, lapsed
  caption without Skip, completed/active captions with Start disabled elsewhere, diagnostic on
  the third skip and from the foreground scan (defer path); `focus.test.tsx`: running/paused
  states, progress bar role, Pause/Finish/Stop, inline rating with the empty state still visible,
  one tap counts, optional second tap, skippable, never re-shown for a rated session.

## 2b. Live routing check on the hosted project (2026-08-27)

Both functions deployed (`supabase functions deploy attribute-rewards` / `plan-request`); the P7
migrations pushed (`20260827150000_p7_feedback`, `20260827160000_p7_sweep_bearer`). `curl` to
`/functions/v1/attribute-rewards` with no headers, or with `apikey` only → **401 from the gateway**
(`{"error":"unauthorized","detail":"missing bearer token"}` — not the handler's message, even
though `verify_jwt = false`); with `Authorization: Bearer <publishable key>` → the handler answers
(401 `{"error":"unauthorized"}`: no backend key configured, no user). Consequence: the cron tick
sends the publishable key as bearer (+ `apikey`) and the third Vault secret is required
(HANDOFF ⛔ 2). Establishes: deploy + routing + the handler's own auth gate. Does not establish:
a successful daily run (needs the Vault secrets) or delivery (needs the host).

**Correction 2026-08-28 — the routing check above was wrong about what it saw.** The 401 it
attributed to attribute-rewards' handler came from **plan-request's** handler: `config.toml` had
`entrypoint = "./functions/plan-request/index.ts"` under `[functions.attribute-rewards]`
(copy-paste), so every deploy of `attribute-rewards` shipped plan-request's bundle, and both
handlers answer a bare `{"error":"unauthorized"}`. Found when the daily call kept returning 401
with the correct key, env and header proven present via a throwaway diagnostic function; confirmed
by `supabase functions download attribute-rewards` (plan-request's files came back). **Lesson for
every future routing check: assert a handler-specific response (e.g. `POST {}` → 400 "mode must
be instant or daily"), never a 401.** Fixed in `config.toml`, redeployed; `handler_test.ts` was
always right — the code had simply never run on the hosted project.

## 2c. Live delivery, cron path and outage behaviour on the hosted project (2026-08-28)

With the VM serving (`https://hourwell-recsys.duckdns.org`), the two function secrets set and the
three Vault secrets created (`hourwell_functions_url`, `hourwell_service_key`, `hourwell_anon_key`
— applied from the box with `vault.create_secret`, not the SQL editor):

- **Daily mode, direct call with the backend key:** 200 `{"mode":"daily","due":24,"users":6,
"tuples_written":24,"delivered":24,…}` — every report `delivery: ok` (the 24 due rows were the
  lapsed recommendations of the P6/P7 smoke users' plans).
- **Cron path end to end:** `attribution_sweep_tick()` → `posted`; `net._http_response` → **200**
  with the handler's JSON; `cron.job` `attribute-rewards-sweep` (`*/15 * * * *`, active) —
  `cron.job_run_details` shows the 19:15 and **19:30 UTC runs `succeeded`**, i.e. pg_cron itself
  delivered after the fix.
- **Storage ↔ service agreement:** `feedback_rewards` 24 rows, `delivered_at` **null count = 0**,
  all `source = daily`; the service's own `recsys_applied_tuples` = 24; VM log shows
  `POST /feedback 200` from the Supabase egress IPs.
- **Learned-path smoke** (P6 script) 18/18 PASS twice, p95 1.5 s — `p6-manual-verification.md` §3.1.
- **UC-03 A1 — outage (19:31 UTC):** `docker compose stop recsys` on the VM (Caddy stays up and
  answers 502) → P6 smoke, 3 runs: **18/18 PASS, `engine = heuristic`, `model_version =
heuristic-p6.0`, `telemetry.ef.reason = fallback:http`**, `ef.total_ms` 1 550 on the first run,
  client timings 1 733 / 1 113 / 1 010 ms — inside the 1.9 s fallback budget; `docker compose
start recsys` → healthy after 15 s → 3 more runs **`learned`** (1 206 / 1 385 / 1 106 ms). The
  reason code is `fallback:http` (a 502 from the reverse proxy), not `timeout|network` as the
  checklist item guessed — a whole-VM outage or a Security-List mistake would produce those; all
  three are the same NFR-R2 path.

## 3. What is NOT established (and where it is tracked)

- ~~Live `/feedback` delivery and the learned-path smoke~~ — **established 2026-08-28 (§2c)**.
- ~~The cron tick reaching the function~~ — **established 2026-08-28 (§2c)**; what remains is a
  real 23:55-local run against a participant's day (the test users' rows were already past due).
- **Device behaviour** — timer under lock/kill, lapse scan after a real background stint,
  VoiceOver/TalkBack on the action row and rating chips, 200 % font on the card with actions,
  offline facts reaching the server once, the device clock across DST, the Android Move picker:
  device-checklist "Feedback loop (P7)".
- **λ_f retune** (revisit, scheduled P7) — deferred to the first data review: no observed q̂
  scales exist without live feedback.
- **UC-04 walk on the simulator** — not done in P7 (the P6 note applies: the simulator adds
  nothing the jest render tests do not already show; the device pass is where it counts).

## 4. Adversarial pass (fresh-context subagent, 2026-08-27)

The reviewer ran all three suites (green, counts matched), then attacked invariants, idempotency,
time, the mapping edge cases, the estimator, the blend, the client, UI rules, security and the
docs. **7 MAJOR + 14 MINOR** findings; all MAJORs and 12 MINORs fixed on the branch before merge,
2 MINORs recorded for later phases. The fix round is the commits `fix(edge)`, `fix(mobile)`,
`fix(db)` after `ea2aeee`.

| #   | Sev.  | Finding (scenario)                                                                                                                                       | Fix                                                                                                                                                                                                                                              |
| --- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | MAJOR | A completion that syncs AFTER the 23:55 job (offline overnight) was ignored: the stored `lapsed` blocked the instant row and no correction path existed. | `upgrade()` in `rewards.ts`: an uncorrected stored `lapsed`/`off_slot`/`partial` inside the 7-day window is rewritten in place by a strictly better instant outcome (`correction = true` → rebuild, original `attributed_at`). Tests `#1`, `#2`. |
| 2   | MAJOR | A row-3 `partial` (abandon at f = 0.1) froze the block: a restart + finish inside the slot was ignored.                                                  | Row 3 is emitted only once the slot can no longer be resumed (`now > slot_end + 15 min` or daily mode); earlier restarts reach rows 1/2; a stored partial is upgradable (#1).                                                                    |
| 3   | MAJOR | Override + session in ONE offline batch scored the session against the OLD slot/features.                                                                | `mapUser` continues with the moved rec (slots, bucket, features, local day) after the override step; test `#3`.                                                                                                                                  |
| 4   | MAJOR | A target outside 06–24 (no bucket) left the server row unmoved (client moved) → wrong window.                                                            | The latest move always patches the slot; the pair/bucket/features only when a bucket exists (`targets` may hold `null`); test `#4`. ADR-0010 §6 amended.                                                                                         |
| 5   | MAJOR | A second move was neither rewarded nor APPLIED (server slot stuck at the first target).                                                                  | Latest move always applied (handler computes the target for the latest fact; `alreadyThere` short-circuit); one pair per placement kept; test updated.                                                                                           |
| 6   | MAJOR | Daily sweep vs in-flight instant call: `ignoreDuplicates` insert + unconditional patch → tuple `lapsed` with status `completed`.                         | `gatePatches`: after writing, re-read the stored tuples and patch a row only when the stored (rec, kind) is the one this pass computed; test `#6`.                                                                                               |
| 7   | MAJOR | A same-day re-plan after "Start" expired the in-progress row (the `accepted` status op was never pushed) — no reward, task double-placed.                | `factsPush` now pushes `recommendation_status` ops (guard rejections ack as moot); `planRequest` pushes facts+statuses (without the instant call) before planning.                                                                               |
| 8   | MINOR | Blend trajectory order-dependent on `attributed_at` ties (online vs rebuild).                                                                            | Total order `(attributed_at, recommendation_id, kind)` in `apply_feedback`, `rebuild_all`, `load_tuples`, `loadUndelivered`.                                                                                                                     |
| 9   | MINOR | Client computed `fraction`/`start_offset_minutes` in payloads (invariant 1 in spirit).                                                                   | Removed; test asserts no `fraction` key.                                                                                                                                                                                                         |
| 10  | MINOR | `loadFacts` loaded 2000 events of all P7 types ascending → newest dropped for heavy users.                                                               | Reward-bearing types only, newest-first with the cap, then reversed.                                                                                                                                                                             |
| 11  | MINOR | Device zone ≠ profile zone silently turned off-slot into lapsed.                                                                                         | Every fact carries `context.tz`; a considered fact under another zone makes the outcome `excluded = timezone_mismatch` (§3.4.2 ambiguity). Test `#11`.                                                                                           |
| 12  | MINOR | Midnight edges: `focus_end.local_day` was the END day; a 23:30–23:59 block was due at the 00:00 tick before its fact could sync.                         | `focus_end` anchored on the session START day; `attribution_due` requires `slot_end + 15 min ≤ p_now` (migration `…170000`, pgTAP r9).                                                                                                           |
| 13  | MINOR | "I did it" on a stored `partial` was dropped.                                                                                                            | `correction()` accepts `lapsed`/`partial`/`off_slot`.                                                                                                                                                                                            |
| 14  | MINOR | The lapse scan lapsed chunk A while a session ran on chunk B of the same task.                                                                           | Sessions looked up by task; test added.                                                                                                                                                                                                          |
| 15  | MINOR | Diagnostic trigger differed between skip (≥ 3) and lapse (multiples of 3).                                                                               | One rule `diagnosticDueFor()` (every third); test extended.                                                                                                                                                                                      |
| 16  | MINOR | Move into the past / Android picker re-opening on every render.                                                                                          | DAO lifts a past target to the next 15-min tick; picker `minimumDate`, Android dialog unmounted after a pick.                                                                                                                                    |
| 17  | MINOR | A session left running for hours: f = 1, EWMA polluted, Start blocked, block never lapses.                                                               | `abandonStaleSessions()` on foreground (planned × 2 + 60 min); the estimator skips samples > 4 × est.                                                                                                                                            |
| 18  | MINOR | `focus_sessions.user_id` not rewritten on sign-in (pre-auth session vanished).                                                                           | Added to the account-transition rewrite list.                                                                                                                                                                                                    |
| 19  | MINOR | Override occupancy counted lapsed/rejected/displaced rows.                                                                                               | Filtered to open + completed placements.                                                                                                                                                                                                         |
| 20  | MINOR | pg_net responses unread; one invalid `profiles.timezone` stalled the sweep.                                                                              | `attribution_due` joins `pg_timezone_names`; `profiles.timezone` validated by trigger (pgTAP); HANDOFF gotcha with the `net._http_response` query.                                                                                               |
| 21  | MINOR | Docs drift (HANDOFF "PR #8" before it existed; ADR-0010 §6 wording; explainer "archive (reversible)" with no unarchive UI).                              | PR #8 exists; ADR-0010 §6/§3/§7 amended; explainer reworded (restore from the task sheet is P9).                                                                                                                                                 |

Deferred (revisit.md): a second move as a _correction_ of the pair (P9/P11); a per-user
advisory lock around map+write as a stronger answer to #6 (P8, when sync-resolve shares the
path). The reviewer's "confirmed OK" list: RLS/grants and service-only functions, invariants
3/4/6 on the service, idempotency keys and re-delivery, the estimator guards and H1 symmetry of
the scaling, the DST behaviour, fact-derived statuses never pushed as status ops, UI rules
(skip never red, neutral captions, inline rating).

Gates after the fix round: typecheck/lint/prettier clean · jest **290 passed** · expo-doctor
21/21 · Deno **98 passed**, lint/fmt clean · ruff/mypy clean, pytest **135 passed** · pgTAP
**32 assertions** (CI) · `database.ts` unchanged by the two follow-up migrations.
