[Unit]
Description=Hourwell RecSys pull-based rollout (docker compose pull && up)
After=docker.service network-online.target
Requires=docker.service

[Service]
Type=oneshot
User=ubuntu
ExecStart=/usr/local/bin/hourwell-rollout
